# ── Core Framework ───────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.31.0
gunicorn==21.2.0
python-multipart==0.0.12

# ── AI / Gemini (reverted to stable google-generativeai) ───────────
google-generativeai>=0.7.2

# ── LangChain + RAG ─────────────────────────────────────────────────
langchain==0.2.16
langchain-google-genai==1.0.10
langchain-community==0.2.16
langchain-core==0.2.43
faiss-cpu==1.9.0.post1

# ── Database ──────────────────────────────────────────────────────────
sqlmodel==0.0.22
psycopg2-binary==2.9.9
SQLAlchemy==2.0.36

# ── Auth & Security ───────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# ── Config & Utilities ────────────────────────────────────────────────
pydantic-settings>=2.4.0
python-dotenv==1.0.0
aiofiles>=23.1.0
requests==2.31.0
httpx==0.27.2
psutil==5.9.8
# razorpay payments: handled via direct HTTP calls to api.razorpay.com using requests (below)
# razorpay==1.4.2  ← NOT required; payment.py uses requests directly to avoid pkg_resources issues

# ── PDF ───────────────────────────────────────────────────────────────
PyPDF2==3.0.1

# ── Rate Limiting ─────────────────────────────────────────────────────
slowapi==0.1.9

# ── Payments ───────────────────────────────────────────────────────────
stripe>=7.0.0

# ── Real-time (Socket.io) ─────────────────────────────────────────────
python-socketio==5.11.0
python-engineio>=4.8.0
eventlet==0.33.3

# Invalidate cache for redeploy 3cryptography>=41.0.0


