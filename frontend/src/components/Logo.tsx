/**
 * Logo.tsx — Re-exports from TulasiLogo (single source of truth).
 * All existing `import { Logo } from "@/components/Logo"` continue to work.
 */
export { Logo, TulasiLogo } from "./TulasiLogo";
