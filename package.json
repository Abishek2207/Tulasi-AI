{
  "name": "tulasi-ai-root",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix frontend && cd backend && pip install -r requirements.txt",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && venv\\Scripts\\activate && python -m uvicorn app.main:app --port 10000 --reload\"",
    "backend": "cd backend && venv\\Scripts\\activate && python -m uvicorn app.main:app --port 10000 --reload",
    "frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
